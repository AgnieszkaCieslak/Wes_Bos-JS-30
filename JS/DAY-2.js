const log = (param, text = "RESULT: ") => {
    console.log(text, param);
  };
  log(true, `test`);
  log(`---------------------------------------------------`);
  log(`JAVA_SCRIPT_30---------------DAY 2`);
  